<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet" />
    <title><%= title %></title>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gray-50">
    <div class="w-full max-w-md mx-auto p-4 bg-white rounded-lg shadow-md">
      <%- include('header.ejs') %>
      <p class="py-2 text-lg font-semibold text-center">
        Welcome to the Learning Management System
      </p>
      <p class="py-2 text-center">
        New here? <a href="/signup" class="text-blue-600 underline">Sign-up now</a>
      </p>
      <p class="py-2 text-center">
        Already have an account? <a href="/login" class="text-blue-600 underline">Sign-in</a>
      </p>
    </div>
  </body>
</html>
